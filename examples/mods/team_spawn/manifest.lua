-- manifest.lua — паспорт мода. Модлоадер читает его первым и грузит ТОЛЬКО перечисленные здесь файлы.
return {
    id = "team_spawn",

    name = "Team Spawn",
    version = "1.0.0",
    author = "Illia",
    description = "Союзники начинают в одной деревне, а шахт рядом столько, сколько нужно на всех.",

    enabled = true,

    -- shared — код выполняется у ВСЕХ, включая клиентов сетевой игры.
    -- Так надо, потому что мод создаёт объекты (жилы) и двигает юнитов: в сетевой игре каждая
    -- сторона считает партию сама, и мир должен получиться одинаковым у всех. Если это сделает
    -- только хост, у клиента разъедутся номера объектов и игра развалится (leInvalidSynch).
    shared = "server.lua",

    -- "required" — меняет ход партии, поэтому в сети мод должен стоять у всех.
    multiplayer = "required",
}
