-- manifest.lua — паспорт мода. Модлоадер читает его первым и грузит ТОЛЬКО перечисленные здесь файлы.
-- Манифест исполняется в пустом окружении: здесь только данные, вызывать функции нельзя.
return {
    -- Уникальный идентификатор: латиница, цифры, _. По нему мод виден в логах и .mods.
    id = "example_mod",

    name = "Example Mod",
    version = "1.0.0",
    author = "Illia",
    description = "Пример мода: события, чтение и запись ресурсов, вызов нативов.",

    -- false — мод лежит в папке, но не загружается.
    enabled = true,

    -- Точка входа: выполняется при загрузке мода.
    entry = "main.lua",

    -- Остальные файлы мода. Подключаются через require("utils") / require("lib.math").
    -- Всё, чего нет в этом списке, загрузить нельзя. Только .lua, только внутри папки мода.
    files = {
        "utils.lua",
    },
}
