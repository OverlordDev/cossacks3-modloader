-- manifest.lua — паспорт мода. Модлоадер читает его первым и грузит ТОЛЬКО перечисленные здесь файлы.
-- Манифест исполняется в пустом окружении: здесь только данные, вызывать функции нельзя.
return {
    -- Уникальный идентификатор: латиница, цифры, _. По нему мод виден в логах, .mods и в сети.
    id = "example_mod",

    name = "Example Mod",
    version = "1.1.0",
    author = "Illia",
    description = "Пример мода: клиент по F6 просит у сервера золото, сервер решает и сообщает всем.",

    -- false — мод лежит в папке, но не загружается.
    enabled = true,

    -- Две стороны мода (можно указать только одну):
    --   server — правила игры: ресурсы, характеристики, код игры. Работает там, где решается игра:
    --            в одиночной игре и у хоста сетевой игры.
    --   client — у каждого игрока (и у хоста): интерфейс, клавиши, эффекты. Только читает состояние игры,
    --            изменения просит у сервера через net.send.
    server = "server.lua",
    client = "client.lua",

    -- Общие модули. Подключаются через require("utils") на любой стороне (у каждой стороны свой экземпляр).
    -- Всё, чего нет в этом списке, загрузить нельзя. Только .lua, только внутри папки мода.
    files = {
        "utils.lua",
    },

    -- "required" — в сетевой игре мод должен быть у всех с той же версией (меняет правила игры).
    -- "optional" — только клиентский мод (интерфейс), другим игрокам не нужен.
    multiplayer = "required",
}
