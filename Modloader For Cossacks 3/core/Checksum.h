#pragma once

// Хеш скриптов, который сверяет лобби (StateMachineLibraryCodeMD5Checksum → gstring_checksum, init.script).
// Игра считает MD5 от исходного текста всех состояний всех state machine проекта — туда попадают и наши
// вставки событий, и состояния ModLoader.*, поэтому с модлоадером хеш не совпадал бы ни с кем.
// Перехватываем натив и считаем тем же алгоритмом и теми же MD5 движка, но без наших изменений:
// для лобби игра с модлоадером выглядит как чистая. Совместимость модов проверяет модлоадер сам.
namespace Checksum
{
    bool Install();
    void Print(); // .checksum: хеш движка (с нашими изменениями), чистый и сохранённый игрой
}
