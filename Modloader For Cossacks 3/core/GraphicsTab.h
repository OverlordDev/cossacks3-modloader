#pragma once

// Вкладка «Graphics» в меню модлоадера: те же крутилки, что и gfx в Lua, только мышкой.
// Рисуется из Overlay (главный поток игры), поэтому зовёт движок напрямую.
// Кнопка Copy as Lua отдаёт текущие значения готовым куском кода для мода.
namespace GraphicsTab
{
    void Draw();
}
