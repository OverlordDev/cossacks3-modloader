#pragma once

// Горячие клавиши, отправляющие команды во встроенный командный процессор игры (_misc_ProcessMessage),
// тот же, что обрабатывает текст из чата. Команда res работает только в одиночной игре или у хоста;
// игра при этом отключает достижения.
namespace Cheats
{
    void Update(); // вызывается из цикла модлоадера
}
